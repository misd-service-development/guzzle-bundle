framework:
    secret:        test
    router:        { resource: "%kernel.root_dir%/%kernel.test_case%/routing.yml" }
    test: ~
    default_locale: en
    session:
        storage_id:     session.storage.mock_file
